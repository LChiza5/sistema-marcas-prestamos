# Copiar este archivo como .env antes de ejecutar el backend.

PUERTO=3000

# Base de datos (debe coincidir con el .env de docker-compose)
DB_HOST=localhost
DB_PORT=3307
DB_USER=app_user
DB_PASSWORD=app_password
DB_NAME=marcas_prestamos

# Sesiones
SESSION_SECRET=cambiar_esta_clave_por_una_larga_y_aleatoria
SESSION_MINUTOS=60

# Origen permitido para el frontend
ORIGEN_FRONTEND=http://localhost:5173
